// Comments

// anInt int;
// aPointer int^ = @anInt;
// anotherInt int = ^aPointer;

if(1+2 != 3+4*anInt){
	f1();

}else if(c2){
	for f2(1, 2, 3);
	while(--anInt > 0) f2(4, 5, 6);
	do f2(7, 8, 9); while(false);

}else{
	f3();
}

// aShort short = 1;
// anExpression long = (aShort = 2), 1 + -aShort * 3 * (4 + 5);

// {
// 	aString string;
// 	aString = "I am a string";
// 	{
// 		aCharPointerArray uchar^[] = "things";	
// 	}
// }

// aFunctionCall(thing1, 1234, "strings fo' days");

// func aFunctionDecl();

// func aFunctionDef(arg1 int^, /*arg2*/ string[]) -> int {
// 	return ^arg1;
// }

// // func thing(arg1 int^, arg2 uint[4]) -> uint[]{
// // 	arg2[0] = ^arg1;
// // 	^arg1 = arg2[1]; 	// Error because lvalues
// 						// can only be either an identifier
// 						// or array subscripts
// // 	return arg1;
// // }

// // Parenthesis optional
// func main { 
// 	thing();
// }